# Environment variables for production (Vercel)
# Add these in Vercel dashboard: Settings > Environment Variables

# Backend URL (your Render deployment URL)
# VITE_BACKEND_URL=https://scribble-backend.onrender.com

# WebSocket URL (same as backend URL)
# VITE_WS_URL=https://scribble-backend.onrender.com

# For local development, create .env.local with:
# VITE_BACKEND_URL=http://localhost:8080
# VITE_WS_URL=http://localhost:8080
